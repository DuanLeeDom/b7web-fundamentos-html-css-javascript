// Manipulando o CSS

function clicou() {
    const li = document.querySelector('li');

    // Camel case | a primeira letra é minúscula as outras inicial é maiúscula
    li.style.backgroundColor = '#00F';
    li.style.fontSize = '20px';
}