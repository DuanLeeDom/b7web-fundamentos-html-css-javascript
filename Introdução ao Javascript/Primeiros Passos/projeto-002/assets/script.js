// Tipos de dados

let nome = "Bonieky" // Tipo String (texto)
let idade = 45.5 // Número com ponto decimal 
let logado = true // Valor booleano (pode ser true ou false)
let lista = ["ovo", "massa", "corante"] 
// Array (lista de valores). Apesar de ser um array, o typeof retorna "object", pois em JavaScript arrays são considerados objetos.

let nomeCompleto = { nome: "Bonieky", idade: 90 } 
// Objeto com duas propriedades: "nome" (string) e "idade" (número)

// Para descobrir o tipo de uma variável, use o operador typeof
// console.log(typeof lista) // Retorna "object", mesmo sendo um array